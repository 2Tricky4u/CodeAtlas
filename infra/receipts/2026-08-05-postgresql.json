{
  "phase": "4-postgresql",
  "date": "2026-08-05",
  "installs": [
    {
      "tool": "postgresql",
      "version": "17.10",
      "method": "winget install --exact --id PostgreSQL.PostgreSQL.17 --override unattended (no pgAdmin/stackbuilder)",
      "service": "postgresql-x64-17 (auto-start, running)"
    }
  ],
  "hardening": {
    "password_encryption": "scram-sha-256 (verified)",
    "pg_hba": "loopback-only (127.0.0.1/32, ::1/128) scram for all rules (verified via pg_hba_file_rules)",
    "listen_addresses": "localhost — VERIFIED 2026-08-05 after service restart. SHOW listen_addresses returns 'localhost'; Get-NetTCPConnection confirms listeners on 127.0.0.1 and ::1 only; no firewall rule exposes 5432.",
    "roles": "codeatlas_migrator (DDL owner of codeatlas + codeatlas_test), codeatlas_app (CONNECT + DML via default privileges), both NOSUPERUSER NOCREATEDB NOCREATEROLE",
    "secrets": "postgres_super / codeatlas_app / codeatlas_migrator passwords in Windows Credential Manager under service 'codeatlas/db' (via keyring); never in files"
  },
  "pending": [
    "nightly pg_dump scheduled task + weekly restore --list verification (M15 observability hardening)"
  ]
}
